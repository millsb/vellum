import * as React from "react";
import PropTypes from "prop-types";
import "./foo-bar.scss";

const FooBar = ({}) => {
    return (
        <div>FooBar</div>
    );
};

FooBar.propTypes = {
}

export default FooBar;